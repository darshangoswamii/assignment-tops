//3. WAP to find reverse of string using recursion
#include<stdio.h>
int main()
{
	int arr[100],size,i;
	printf("\nEnter the size of array : ");
	scanf("%d",&size);

	for (i = 1; i<=size ; i++)
	{
		printf("\Enter the arr[%d]",i);
		scanf("%d",&arr);
		
	}
	printf("original array %d",arr);
	
}
